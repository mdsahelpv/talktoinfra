Thank you for installing {{ .Chart.Name }}.

Your release is named {{ .Release.Name }}.

## Accessing the Application

{{- if .Values.ingress.enabled }}

The application is available at:
{{- range .Values.ingress.hosts }}
  - http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}

{{- else if eq .Values.services.nginx.service.type "LoadBalancer" }}

Get the external IP:
  kubectl get svc --namespace {{ .Release.Namespace }} {{ include "opsai.fullname" . }}-nginx

{{- else if eq .Values.services.nginx.service.type "NodePort" }}

The application is available on:
  http://<NodeIP>:{{ .Values.services.nginx.service.nodePort }}

Get the Node IP:
  kubectl get nodes -o wide

{{- else }}

To access the application, run:
  kubectl port-forward --namespace {{ .Release.Namespace }} svc/{{ include "opsai.fullname" . }}-nginx 8080:80

Then visit: http://localhost:8080

{{- end }}

## Default Credentials

{{- if .Values.secrets.create }}
{{- if not .Values.secrets.admin.password }}

⚠️  Admin password was auto-generated. Get it with:
  kubectl get secret --namespace {{ .Release.Namespace }} {{ include "opsai.fullname" . }}-secrets -o jsonpath="{.data.ADMIN_PASSWORD}" | base64 --decode

{{- end }}

Username: {{ .Values.secrets.admin.username | default "admin" }}

{{- end }}

## Ollama Setup

{{- if .Values.ollama.enabled }}
{{- if not .Values.ollama.external.enabled }}

Pull your first model:
  kubectl exec -it --namespace {{ .Release.Namespace }} deploy/{{ include "opsai.fullname" . }}-ollama -- ollama pull llama2

{{- else }}

Using external Ollama at: {{ .Values.ollama.external.url }}

{{- end }}
{{- end }}

## Useful Commands

Check pod status:
  kubectl get pods --namespace {{ .Release.Namespace }}

View logs:
  kubectl logs --namespace {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "opsai.name" . }}

Upgrade the release:
  helm upgrade {{ .Release.Name }} {{ .Chart.Name }} --namespace {{ .Release.Namespace }}

Uninstall:
  helm uninstall {{ .Release.Name }} --namespace {{ .Release.Namespace }}

## Support

For more information, visit:
  - Documentation: https://github.com/yourorg/opsai/blob/main/docs/
  - Issues: https://github.com/yourorg/opsai/issues
