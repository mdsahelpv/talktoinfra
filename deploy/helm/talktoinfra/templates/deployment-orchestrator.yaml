apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "talktoinfra.fullname" . }}-orchestrator
  labels:
    {{- include "talktoinfra.labels" . | nindent 4 }}
    app.kubernetes.io/component: orchestrator
spec:
  replicas: {{ .Values.orchestrator.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "talktoinfra.name" . }}
      app.kubernetes.io/component: orchestrator
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "talktoinfra.name" . }}
        app.kubernetes.io/component: orchestrator
    spec:
      {{- include "talktoinfra.imagePullSecret" . | nindent 6 }}
      serviceAccountName: {{ include "talktoinfra.serviceAccountName" . }}
      containers:
        - name: orchestrator
          image: "{{ .Values.global.imageRegistry }}/{{ .Values.orchestrator.image.repository }}:{{ .Values.orchestrator.image.tag | default .Values.global.imageTag }}"
          imagePullPolicy: {{ .Values.orchestrator.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.orchestrator.service.port }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ include "talktoinfra.fullname" . }}-config
            - secretRef:
                name: {{ include "talktoinfra.fullname" . }}-secrets
          {{- with .Values.orchestrator.env }}
          env:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml .Values.orchestrator.resources | nindent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
